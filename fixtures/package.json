{
  "name": "@wirelens/fixtures",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node --experimental-strip-types src/build-handshake.ts && node --experimental-strip-types src/build-phase2-captures.ts",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "22.15.30",
    "prettier": "3.9.6",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  }
}
